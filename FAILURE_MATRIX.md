# DINELY — SYSTEM FAILURE MATRIX (FAILURE_MATRIX.md)
**Document Status:** Complete Forensic Trace of All Broken Flows  
**Severity Scale:**
- **P0**: Complete system/platform operational failure; critical path blocked
- **P1**: Major business workflow broken; data loss or isolation breach
- **P2**: Significant operational defect or degradation; workaround exists
- **P3**: Minor defect, UI inconsistency, or technical debt cleanup

---

## 1. Failure Matrix Table

| Severity | Flow | Current Behavior | Expected Behavior | Root Cause | Dependencies | File / Component | Database Table | API Endpoint |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **P0** | **Tenant Subdomain Resolution** | Navigating to `https://<slug>.dinely.food` produces `DNS_PROBE_FINISHED_NXDOMAIN` or `Could not resolve host` | Request routes to tenant frontend with tenant context resolved | DNS nameservers for `dinely.food` point to GoDaddy (`domaincontrol.com`); no wildcard `*.dinely.food` DNS record exists; Cloudflare Worker is inactive | GoDaddy DNS, Cloudflare Worker | `cloudflare/dinely-tenant-router/src/index.js`, DNS Nameservers | `restaurant_domains` | `GET https://<slug>.dinely.food` |
| **P0** | **QR Code Customer Scanning** | Scanning physical table QR opens `https://<slug>.dinely.food/customer?table=...` which fails at DNS lookup | Phone browser opens the customer ordering menu for that specific restaurant and table | QR URL generator creates subdomains that cannot resolve under current GoDaddy DNS configuration | DNS, QR Generator, Frontend Routing | `frontend/src/packages/ui/QRCodeDisplay.tsx:206`, `backend/app/modules/restaurants/router.py:315` | `tables (qr_code_url)` | None (DNS failure) |
| **P0** | **Restaurant Creation Backend Sync** | Owner submits restaurant creation in wizard; if Render backend is sleeping or times out, restaurant is created ONLY in browser `localStorage` | Creation must execute an atomic backend transaction in PostgreSQL; failure must display explicit network error to owner | `client.ts:1545` wraps `fetch('/restaurants')` in a catch block that swallows errors and returns locally generated mock restaurant | Render cold start, `client.ts` mock DB | `frontend/src/packages/api/client.ts:1545`, `frontend/src/apps/owner/OnboardingWizard.tsx` | `restaurants` | `POST /api/v1/restaurants` |
| **P0** | **Platform Admin Application Visibility** | Newly created restaurants do not appear in Platform Admin pending queue if backend was bypassed by frontend fallback | Platform Admin displays all submitted restaurants across the entire system in real time | When creation fails to reach PostgreSQL, it exists only in the creator's browser `localStorage`; Admin queries PostgreSQL via Render API and sees nothing | Render API, Dual DB architecture | `frontend/src/apps/platform/PlatformApp.tsx:158`, `backend/app/modules/platform/router.py:68` | `restaurants` | `GET /api/v1/admin/restaurants` |
| **P1** | **Tenant Authorization Spoofing** | Any client can send `X-Staff-Role: WAITER` and `X-Staff-Restaurant-Id: rest-xxx` to execute staff-restricted backend APIs | Backend must cryptographically verify an authenticated session/JWT token before granting staff privileges | `tenant_auth.py:106-115` extracts caller role and restaurant ID directly from untrusted HTTP headers when no Bearer token is present | Tenant Guard, RBAC | `backend/app/core/security/tenant_auth.py:106-115` | `restaurants` | All staff-protected `/api/v1/*` routes |
| **P1** | **Cross-Tenant Session Bleed in Customer App** | Guest customer opening `/customer` without a resolved subdomain attaches to whatever restaurant was previously active on that device | Guest customer must receive an error or explicit tenant selection prompt; never attach to arbitrary restaurant | `CustomerApp.tsx:223` calls `api.getCurrentRestaurantId()` which reads `localStorage['dinely_active_restaurant_id']` | Frontend resolver, Browser storage | `frontend/src/apps/customer/CustomerApp.tsx:223`, `frontend/src/packages/api/client.ts:507` | None | None |
| **P1** | **Table Session ID Assignment in Orders** | If order payload passes slug as `restaurantId`, `Order` and `TableSession` are created with `restaurant_id = slug` instead of canonical `restaurant.id` | All orders and sessions must strictly reference the canonical primary key `Restaurant.id` | `orders/router.py:183, 260` uses `payload.restaurantId` directly instead of resolved `restaurant.id` | Database integrity, Order router | `backend/app/modules/orders/router.py:183, 260` | `orders`, `table_sessions` | `POST /api/v1/orders` |
| **P1** | **Canonical Public Domain Platform Leakage** | `getCanonicalPublicDomain()` called on apex domain produces `https://dinely.food` instead of `https://<slug>.dinely.food` | Must always produce the isolated tenant URL format `https://<slug>.dinely.food` | `client.ts:69` falls back to `window.location.origin` when no subdomain is detected | Domain utility | `frontend/src/packages/api/client.ts:69` | `restaurant_domains` | `GET /api/v1/restaurants/public/resolve` |
| **P2** | **Startup Auto-Archiving of Test Records** | When Render backend boots, raw SQL archives restaurants matching `id LIKE 'rest-test-%'` or `owner_email IS NULL` | Database records should only be updated by explicit user or admin action, not startup DDL scripts | `main.py:104-114` executes blanket `UPDATE restaurants SET lifecycle_status = 'ARCHIVED'` on every backend startup | Backend Lifespan, Startup SQL | `backend/app/main.py:104-114` | `restaurants` | None (Server startup) |
| **P2** | **Kitchen & Bar Realtime Disconnects** | Kitchen and Bar terminals stop receiving new orders after ~55 seconds of inactivity | Realtime connection must remain alive via automatic ping/pong or transparent reconnect with event backfill | Render free-tier drops idle WebSocket connections after 55s; client reconnects but misses orders created during downtime | WebSockets, Render timeout | `frontend/src/packages/api/realtime.ts`, `backend/app/modules/websocket/manager.py` | `orders` | `WSS /api/v1/ws/{restaurant_id}` |
| **P2** | **Multiple Conflicting Lifecycle Status Fields** | A restaurant can have `is_approved = True` while `lifecycle_status = 'PENDING_APPROVAL'` and `status = 'CLOSED'` | System must maintain a single authoritative lifecycle enum: `DRAFT`, `PENDING_APPROVAL`, `LIVE`, `REJECTED`, `ARCHIVED` | Three separate columns exist on `Restaurant` model (`is_approved`, `lifecycle_status`, `status`) without database-level sync constraints | Data modeling, Admin router | `backend/app/modules/restaurants/models.py:34-41` | `restaurants` | `POST /api/v1/admin/restaurants/approve` |
| **P3** | **Legacy Domain References in Sanitizers** | Code contains sanitizers cleaning `.dinely.app` and `?tenant=` query strings across multiple components | Clean codebase with single canonical domain pattern | Lingering legacy compatibility code from previous iterations | Codebase hygiene | `frontend/src/packages/ui/QRCodeDisplay.tsx:206`, `backend/app/main.py:102` | `tables`, `restaurants` | None |
| **P3** | **Client-Side Storage Bloat** | `localStorage['dinely_production_db_v3']` grows indefinitely with mock orders, tickets, and tables | Only session authentication tokens and UI preferences should reside in client storage | Entire database mirror serialized to JSON and saved on every state mutation | Client architecture | `frontend/src/packages/api/client.ts:440-480` | None | None |
| **P3** | **Modal Z-Index Stacking Defects** | Modals in Owner Workspace and Platform Admin occasionally render behind parent cards or headers | Modals should render in high-level portal overlay | Inconsistent Tailwind and custom CSS `z-index` values without central portal root | UI Design System | `frontend/src/packages/ui/Modal.tsx` | None | None |
